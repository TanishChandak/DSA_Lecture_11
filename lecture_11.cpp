#include<iostream>
using namespace std;
int main()
{
    // Time and Space Complexity:
    //Constant time = O(1) (0 < 10)
    // Linear time = O(n); (0 < n)
    // logarithmic time = O(log n); Used in the binary search
    // Quadratic time = O(n^2); Used in nested for loop
    // Cubic time = O(n^3); Used in nested for loop
}